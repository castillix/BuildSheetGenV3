version: '3.8'

services:
  buildsheet:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./cpus.db:/app/cpus.db
      - ./prices.txt:/app/prices.txt
      - ./generated:/app/generated
    environment:
      - SECRET_KEY=your-secret-key-change-me
